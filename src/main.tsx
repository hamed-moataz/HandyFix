import ReactDOM from "react-dom/client";
import RouterHandler from "./router/RouterHandler.tsx";
import "./global.scss";

ReactDOM.createRoot(document.getElementById("root")!).render(<RouterHandler />);
